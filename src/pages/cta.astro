---
export const prerender = false;

if (Astro.request.method === 'POST') {
    try{
        const data = await Astro.request.formData();
        const formCsrfToken = data.get('csrfToken');
        console.log('formCsrfToken: ', data);
    }catch(error){
        console.log('Error on cta astro: ', error)
    }
}
---
